import 'package:flutter/material.dart';

class Create_Ai extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AI 생성 화면'),
      ),
      body: Center(
        child: Text('AI 생성 페이지'),
      ),
    );
  }
}